apiVersion: v1
kind: Service
metadata:
  name: {{ include "product-a-service.fullname" . }}
  labels:
    app: {{ include "product-a-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort | default(3000) }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "product-a-service.name" . }}
